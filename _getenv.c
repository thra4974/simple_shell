/**#include "holberton.h"

char *_getenv(const char *name)
{
	name = "PATH";
	 iterate through env  
	 Search for name string in path.

}
int main(int ac, char **argv, char **envp)
{
	char **ptr_path;
	*ptr_path = _getenv("PATH");
	ptr_path = _strdup(envp);
	
}
 **/
